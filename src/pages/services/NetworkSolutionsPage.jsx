import React from 'react';
import ServiceDetailPage from '../ServiceDetailPage';

export default function NetworkSolutionsPage({ navigate }) {
  return <ServiceDetailPage serviceId="network-solutions-and-services" navigate={navigate} />;
}
