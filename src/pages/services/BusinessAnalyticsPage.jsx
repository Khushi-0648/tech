import React from 'react';
import ServiceDetailPage from '../ServiceDetailPage';

export default function BusinessAnalyticsPage({ navigate }) {
  return <ServiceDetailPage serviceId="business-analytics" navigate={navigate} />;
}
